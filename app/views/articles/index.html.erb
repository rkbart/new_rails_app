<div style="display: flex; flex-direction: column; align-items: center">
  <h1>Articles</h1>

    <% @articles.each do |article| %>
      <strong><p><%= link_to article.title, article %></p></strong>
    <% end %>


  <%= link_to "Create New Article", new_article_path, class: "btn"%>
</div>